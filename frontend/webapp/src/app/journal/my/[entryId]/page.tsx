export const metadata = {
  title: "Journal",
};

/**
 * Entry deep-link / mobile editor route. Static siblings
 * (`gratitudes`, `insights`) take precedence in the App Router.
 * UI is owned by the persistent Journal layout shell.
 */
export default function JournalEntryPage() {
  return null;
}
