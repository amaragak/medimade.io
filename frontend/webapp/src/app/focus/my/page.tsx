import { FocusTimerView } from "@/components/focus-timer-view";

export const metadata = {
  title: "Focus",
  description: "Pomodoro-style focus timer — stay with one thing.",
};

export default function FocusAppPage() {
  return <FocusTimerView />;
}
